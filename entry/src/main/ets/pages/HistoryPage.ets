@Component
export struct HistoryPage {
  build() {
    Column() {
      Text('历史记录页预留')
        .fontSize(20)
        .fontWeight(FontWeight.Medium)
    }
    .width('100%')
    .height('100%')
    .justifyContent(FlexAlign.Center)
  }
}
